<h1>My Tasks</h1>

<% @tasks.each do |task| %>
  <br><%= check_box_tag :active, "t",checked = task.completed %>
  <%= link_to task.title, task_path(task)%>

<%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>

